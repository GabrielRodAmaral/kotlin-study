package firstSteps

import variables.exerciseTwoVariables

fun main() {
    println("Hello Kotlin!")

    // Aqui chamo uma função da variables que conver tipo de funções númericas
    // tradingtypevariables()

    // Aqui chamo uma função da variables que explica variáveis númericas unsigned
    // nosignalorvaluevariables()

    // Aqui chamo uma função da variables que explica declaração de variáveis booleanas no kotlin
    // Aqui chamo uma função da variables que explica variaveis númericas unsigned

    // Aqui chamo uma função da variables explicando variáveis char no kotlin
    // explica também .inc(), .dec(), .isDigit(), isUpperCase(), isLowerCase(), .digitToInt()
    // charvariables()

    // Aqui chamo uma função da variables explicando variáveis String no kotlin
    // explica também .length
    // stringvariables()

    // Aqui chamo uma função da variables explicando String literals no kotlin
    // explica também \n e uso da \, .trimIndent(), .trimMargin(), raw strings
    // stringliterals()

    /*
    const val => quando você declara uma val global o ideal é declarar como const val, isso irá te dar um ganho
    de performance porque ele vai declarar essa variável na fase de compilação sem necessidade de acessala em tempo
    de execução
 */

    // Aqui chamo uma função da variables explicando como ler dados inseridos digitados pelo usuário
    // explica também .reversed() com strings, readln(), scanner
    // variables.digitedkt()

    // Aqui chamo o primeiro exercício do modulo de variáveis e tipos de dados
    // exerciseOneVariables()

    // Aqui chamo o segundo exercício do modulo de variáveis e tipos de dados
    // exerciseTwoVariables()

    //                          Início do modulo de operadores

    // Aqui chamo uma função do operators explicando operadores matemáticos básicos
    // soma, subtração, divisão, multiplicação, modal
    // operators.firstOperators()

    // Aqui chamo uma função do operators explicando operadores relacionais (>, <, ==)
    // operators.relationalOperators()

    // Aqui chamo uma função do operators explicando operadores lógicos (OR, AND, NOT)
    // operators.logicalOperators()

    // Aqui chamo uma função do operators explicando operadores de atribuição
    // operators.assignmentOperators()

    // Aqui chamo uma função do operators explicando operadores de pós incremento e pré incremento
    // operators.incDecOperators()

    // Aqui chamo uma função do operators explicando precedência de operadores
    // operators.precedenceOperators()

    // Aqui chamo o primeiro exercício do modulo de operadores
    // operators.exerciseOneOperators()

    // Aqui chamo o segundo exercício do modulo de operadores
    // operators.exerciseTwoOperators()

    //Aqui chamo uma função do decision explicando if, else e else if no kotlin
    // decision.ifElse()

    // Aqui chamo o primeiro exercício do modulo de estruturas de decisão
    // decision.exerciseOneDecisions()

    // Aqui chamo o segundo exercício do modulo de estruturas de decisão
    // decision.exerciseTwoDecisions()

    // Aqui chamo o terceiro exercício do modulo de estruturas de decisão
    // decision.exerciseThreeDecisions()

    // Aqui chamo uma função do decision explicando if retornando valores a uma variável
    // decision.ifReturn()

    // Aqui chamo uma função do decision explicando when no kotlin (like switch)
    // Explica também recurso de range
    // decision.whenStructure()

    // Aqui chamo o quarto exercício do modulo de estruturas de decisão
    // decision.exerciseFourDecisions()

    // Aqui chamo uma função do repeating explicando while no kotlin
    // repeating.whileStructure()

    // Aqui chamo uma função do repeating explicando do while no kotlin
    // repeating.doWhile()

    // Aqui chamo o primeiro exercício do modulo de estruturas de repetição
    // repeating.exerciseOneRepeating()

    // Aqui chamo uma função do repeating explicando for no kotlin com ranges
    // Explica também downTo, step, until
    // repeating.forStructure()

    // Aqui chamo o segundo exercício do modulo de estruturas de repetição
    // repeating.exerciseTwoRepeating()

    // Aqui chamo uma função do repeating explicando jumps em um loop no kotlin
    // Explica continue (pula a repetição do for) e break (para o for por completo)
    // repeating.jumps()

    // Aqui chamo uma função do repeating explicando labels no kotlin
    // repeating.labels()

    // Aqui chamo o terceiro exercício do modulo de estruturas de repetição
    // repeating.exerciseThreeRepeating()

    // Aqui chamo uma função do nullable explicando variáveis com tipo nullable
    // nullable.nullableVariables()

    // Aqui chamo uma função do nullable explicando safe calls com variáveis nulas
    // Explica também smart cast
    // nullable.safeCallsNull()

    // Aqui chamo uma função do nullable explicando o elvis operator
    // nullable.elvisOperator()

    // Aqui chamo uma função do nullable explicando o not null assertion operator
    // nullable.nNassertionOperator()

    // Aqui chamo o primeiro exercício do modulo de nullable types
    // nullable.exerciseOneNullable()

    // Aqui chamo o segundo exercício do modulo de nullable types
    // nullable.exerciseTwoNullable()
}